-fly-template is the base source file
-makefile is makefile
-parameters is default parameters
-experiment*.csv is all experiments to be run
-generate_params.py uses experiment.csv to populate ./experiments
-./experiments contains parameter sets and finished smoldyn files
-./results contains results files
-./source contains:
	cell-shape defines the appearance and properties of the cell membrane
	cell-core allows exclusion of mRNA from the core
	logging controls what's measured
	nuclei holds a list of nuclei to absorb proteins
	runtime is the changes made while the code runs
	species sets properties of the species
	appearance contains everything related to graphics
